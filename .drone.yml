kind: pipeline
type: exec
name: Development Pipeline Exec

platform:
  os: linux
  arch: amd64

trigger:
  branch:
    - development

node:
  env: dev

steps:
- name: deploy
  commands:
  - docker compose up -d --build

- name: docker clean
  commands:
  - docker system prune -a --volumes -f